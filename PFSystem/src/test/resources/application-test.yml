spring:
  datasource:
    url: jdbc:mysql://localhost:3306/PFSystemTest
    username: chiragagarwals
    password: chiragagarwals

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
