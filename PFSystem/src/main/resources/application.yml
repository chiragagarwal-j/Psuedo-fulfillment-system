spring:
  datasource:
    url: jdbc:mysql://localhost:3306/PFSystem
    username: chiragagarwals
    password: chiragagarwals

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false

  mail:
    host: smtp.gmail.com
    port: 25
    username: procharge.foryou@gmail.com
    password: amrh spub ijaj zrlo
    properties:
      mail:
        debug: true
        transport:
          protocol: smtp
        smtp:
          auth: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
          starttls:
            enable: true